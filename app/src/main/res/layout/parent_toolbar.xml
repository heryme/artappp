<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/parent_toolbar"
    android:layout_width="match_parent"
    android:layout_height="?actionBarSize"
    android:background="@color/color_app_base"
    app:contentInsetLeft="0dp"
    app:contentInsetStart="0dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/img_toolbar_back"
            android:layout_width="@dimen/toolbar_back_h_w_40"
            android:layout_height="@dimen/toolbar_back_h_w_40"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:contentDescription="Go Back"
            android:padding="@dimen/padding_10"
            android:src="@drawable/ic_back" />

        <TextView
            android:id="@+id/tv_toolbar_title"
            style="@style/style_toolbar_style"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerInParent="true"
            android:textStyle="bold"/>

        <ImageView
            android:id="@+id/img_toolbar_edit"
            android:layout_width="@dimen/toolbar_back_h_w_40"
            android:layout_height="@dimen/toolbar_back_h_w_40"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:padding="@dimen/padding_10"
            android:visibility="invisible"
            android:src="@drawable/ic_edit" />

    </RelativeLayout>
</android.support.v7.widget.Toolbar>