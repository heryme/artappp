<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--<android.support.design.widget.TabLayout
        android:id="@+id/tabs_sign_up"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:background="@color/color_app_base"
        app:tabGravity="fill"
        app:tabIndicatorColor="@color/color_app_base"
        app:tabIndicatorHeight="3dp"
        app:tabMode="fixed"
        app:tabSelectedTextColor="@android:color/white"
        app:tabTextAppearance="@style/tab_widget_style"
        app:tabTextColor="@color/white_alpha_35" />-->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:weightSum="1.0">

        <TextView
            android:id="@+id/tv_tab_director"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.5"
            android:gravity="center"
            android:text="@string/signup_director_title"
            android:textColor="@color/White"
            android:textSize="@dimen/font_size_18" />

        <TextView
            android:id="@+id/tv_tab_student"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.5"
            android:gravity="center"
            android:text="@string/signup_student_title"
            android:textColor="@color/White"
            android:textSize="@dimen/font_size_18" />

    </LinearLayout>

</LinearLayout>